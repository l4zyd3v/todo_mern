import s from "./categoriesCard.module.css";
import { CategoryCardInterface } from "../../types";

export default function CategoriesCard({
  _id,
  name,
  color,
  userId,
  tasks,
}: CategoryCardInterface) {
  function getTaskAmountBasedOfCategory(tasks) {
    console.log("getTaskAmountBasedOfCategory", tasks);
  }

  return (
    <section className={s.card}>
      <p className={s.tasksAmount}>40 tasks</p>
      <h3 className={s.heading2}>{name}</h3>
      <span className={s.progressBar}>
        <span className={s.progess}></span>
      </span>
    </section>
  );
}
